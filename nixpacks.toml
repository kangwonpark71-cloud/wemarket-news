[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "rm -rf .next/cache", "npm run build", "npx playwright install --with-deps chromium", "echo BUILD_ID=$(date +%s)"]

[start]
cmd = "npm run start"

