[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "rm -rf .next/cache", "npm run build", "echo 'FORCE_REBUILD_20240715'"]

[start]
cmd = "npm run start"
